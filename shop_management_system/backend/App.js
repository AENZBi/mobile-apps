import React from 'react';
import { SafeAreaView } from 'react-native';
import ProductForm from './ProductForm';

const App = () => {
    return (
        <SafeAreaView>
            <ProductForm />
        </SafeAreaView>
    );
};

export default App;
